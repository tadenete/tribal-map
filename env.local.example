# Copy to .env.local (gitignored). Vite merges this over .env — use for local API URL only.

# Local Lambda shim: in aws-lambda-maps-addresses run `npm run dev:local`
VITE_CGRAPI=http://localhost:3456/addresses
